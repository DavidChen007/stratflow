version: '3.8'

services:
  # 1. 数据库服务
  db:
    image: mysql:8.0
    container_name: stratflow-db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: stratflow
    ports:
      - "3306:3306"
    volumes:
      - ./mysql-data:/var/lib/mysql
      - ./setup.sql:/docker-entrypoint-initdb.d/setup.sql
    networks:
      - stratflow-net

  # 2. NestJS 后端服务
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: stratflow-backend
    restart: always
    environment:
      - DB_HOST=db
      - DB_USER=root
      - DB_PASSWORD=root
      - DB_NAME=stratflow
      - API_KEY=${API_KEY}
    depends_on:
      - db
    networks:
      - stratflow-net

  # 3. 前端服务 (入口)
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: stratflow-frontend
    restart: always
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - stratflow-net

networks:
  stratflow-net:
    driver: bridge
